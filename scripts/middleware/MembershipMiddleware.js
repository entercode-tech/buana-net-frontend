class MembershipMiddleware {
  async run() {
    // let user = await window.auth.user();

    // if (user?.membership?.status !== true) {
    //   location.href = "https://www.google.com";
    // }
    myconsole("run the membership middleware");
  }
}
